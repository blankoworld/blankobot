Liste des tâches à faire (TODO LIST)
------------------------------------

- FAIT | faire une liste blanche de personnes autorises a utiliser le robot
- FAIT | permette l'envoi de n'importe quelle commande avec un ! par exemple ou sans
- FAIT | faire une aide contextuelle, comme un !aide => n'afficher que les commandes publiques
- permettre la reconnexion automatique à un canal auquel il vient de se 
faire jarter (pas plus que 2 a trois fois)
- permettre la connexion à plusieurs canaux
- FAIT | permettre l'utilisation d'argument pour le present script : botirc arg1 arg2
- enregistrer les retours dans un fichier ?
- si retour dans un fichier, mais également un fichier de conf, sans compter une
  possibilité d'ajouter des arguments à notre script, pourquoi ne pas faire ceci :
  botirc <fichierConf> <fichierSortie> ?
- FAIT | argument <fichierConf>
- argument <fichierSortie>
- option VDM (viedemerde)
- option d'ajout de news (on affiche les nouvelles du flux RSS d'un site)
- FAIT | option de lancé de dé (!de 20) (pas plus qu'un dé 100)
- FAIT | faire un fichier yaml de configurations diverses et l'adapter en fonction
- permettre l'ajout en direct d'utilisateurs dans le authorized_users =>
  requiert un fichier yaml de configuration
- faire une méthode d'identification sur le robot à l'aide du pseudo et d'un mdp
  --> permet de donner accès à des commandes spécifiques comme OP, VOICE, etc.
- permettre au robot de discuter sur un canal pour dire salut, ça va? moi bien, et toi ?
- permettre la connexion à un serveur sécurisé
- FAIT | CHANGER MOT DE PASSE du compte IRC utilisé pour les tests
- Ouvrir un fichier config.yml si aucun argument donné
- FAIT | Ajouter des paramètres supplémenaires dans le fichier de configuration pour :
  + nom d'utilisateur
  + nom de l'hôte client
  + nom du serveur client
  + nom réel du client
- Ajouter des exemples pour la commande !aide
- Permettre d'ajouter un argument à la plupart des commandes pour :
  + parler sur un canal
  + agir sur un canal
- Modifier les REGEX pour la vérification du pseudo pour evolu et freenode, en ajoutant par exemple dans la REFEX le chiffre 3 directement !
- Peut être en faire une méthode principale pour valider le pseudo soit chez freenode et evolu.net une fois les REGEXP faites
- Et que faire d'une méthode d'identification avec identifiant et mot de passe pour le bot ?


Liste des potentielles FONCTIONS
--------------------------------

- dit #canal :message => similaire à say
- va #canal => similaire à join
- op #canal pseudo
- !de 100
- !google
- !images
- !neuf adresse_flux_rss.xml => similaire à "news"
- FAIT | !aide
- FAIT | faire #canal :message => similaire à act --> PRIVMSG #canal :ACTION s'amuse
  + commande transformée en !il

